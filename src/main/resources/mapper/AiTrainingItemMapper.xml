<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.routerecipt.project.mapper.AiTrainingItemMapper">
	<insert id="insertAiTrainingItem" parameterType="com.routerecipt.project.dto.AiTrainingItemDTO">
	  	insert into ai_training_item (item_text, predicted_label, predicted_confidence, ai_source, model_version, final_label, corrected_yn, used_for_training)
	  	values(#{item_text}, #{predicted_label}, #{predicted_confidence}, #{ai_source}, #{model_version}, #{final_label}, #{corrected_yn}, #{used_for_training})		
	</insert>
	
	<select id="selectTrainingTargets" resultType="com.routerecipt.project.dto.AiTrainingItemDTO">
		select train_id, item_text, final_label from ai_training_item where used_for_training = 'N' and length(item_text) >= 2
	</select>
	
	<update id="markAsTrained">
		update ai_training_item set used_for_training = 'Y', trained_at = NOW() where train_id in
		<foreach collection="list" item="id" open="(" separator="," close=")">
			#{id}
		</foreach>
	</update>
	

</mapper>